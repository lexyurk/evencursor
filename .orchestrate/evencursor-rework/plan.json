{
  "$schema": "../../../home/ubuntu/.cursor/plugins/cache/cursor-public/9333/d1cdb88a9eb33cf392395c87e3fd76419fc1010e/skills/orchestrate/schemas/plan.schema.json",
  "goal": "Rework evencursor (Even Realities G2 plugin) from the overloaded local-CLI bridge into a clean Vite SPA that drives Cursor Cloud Agents via the REST API at https://cursor.com/docs/cloud-agent/api/endpoints, streams voice via Deepgram, and renders to the G2 HUD via @evenrealities/even_hub_sdk. Sign-in = paste-once Cursor API key (no extra OAuth). Recognise slash-commands from the voice stream. Source-of-truth spec is committed at docs/spec.md on the planner branch.",
  "summary": "rework evencursor onto Cursor Cloud Agents + Deepgram + Even Hub HUD",
  "dispatcher": { "firstName": "Alex" },
  "rootSlug": "evencursor-rework",
  "baseBranch": "cursor/orchestrate-evencursor-rework-d7aa",
  "prBase": "main",
  "repoUrl": "https://github.com/lexyurk/evencursor",
  "selfAgentId": "bc-952c06ce-c152-4ea0-997b-94d5cd4cd7aa",
  "syncStateToGit": true,
  "tasks": [
    {
      "name": "scaffold",
      "type": "worker",
      "model": "composer-2.5",
      "scopedGoal": "Reset the repo to the clean layout described in docs/spec.md §3 and §4. This task is plumbing only — no business logic in cursor/voice/glasses modules yet; just exported function stubs that throw `not implemented` so downstream workers can fill them in. Concretely:\n\n1. Delete `src/bridge/` (entire directory), `src/hub/main.ts`, `src/hub/even.ts`, `src/hub/speech.ts`, `src/hub/styles.css`, `src/hub/vite-env.d.ts`, `src/shared/protocol.ts`, `src/shared/protocol.test.ts`, `tsconfig.bridge.json`, and the old `index.html`.\n2. Rewrite `package.json` per spec §4: drop `dotenv`, `ws`, `zod`, `tsx`, `@types/ws`; add `@deepgram/sdk` at its latest 3.x or 4.x; keep `@evenrealities/even_hub_sdk`; replace scripts (drop dev:bridge, start:bridge; add dev/build/preview/typecheck/test).\n3. Rewrite `tsconfig.json` for strict-mode browser TS targeting ES2022 + DOM lib; remove the bridge tsconfig.\n4. Rewrite `vite.config.ts` minimally (`defineConfig({ server: { host: true, port: 5173 } })`).\n5. Create the directory tree exactly as in spec §3: `src/main.ts`, `src/ui/{App,SignIn,AgentsList,AgentDetail,VoiceBar}.ts`, `src/ui/styles.css`, `src/cursor/{client,types,auth}.ts`, `src/voice/{deepgram,mic,commands,types}.ts`, `src/glasses/{adapter,pages}.ts`, `src/storage/storage.ts`, `src/shared/events.ts`, `public/app.json`. Each new .ts file MUST compile under `tsc --noEmit` immediately — export the named functions/types listed in §3 and §11 with empty bodies that `throw new Error('not implemented: <name>')`. Type signatures MUST already be filled in based on the API tables in spec §5/§6/§8 so that the downstream module workers don't have to re-design them.\n6. Implement `src/storage/storage.ts` fully (it's small): a `KeyStore` with `getKey(name: string): Promise<string | undefined>` and `setKey(name: string, value: string | undefined): Promise<void>`, preferring the Even Hub bridge via `waitForEvenAppBridge()` with a 500 ms timeout, falling back to `window.localStorage`. This is the only module other than scaffolding that you implement.\n7. Implement `src/shared/events.ts` fully (it's small): a typed `Emitter<TEvents>` with `on/off/emit`, ~30 lines.\n8. Rewrite `index.html` to a minimal `<div id=\"app\"></div>` + `<script type=\"module\" src=\"/src/main.ts\"></script>`.\n9. Rewrite `README.md` to match the new architecture (drop the old API curl examples that referred to the local bridge; add Even Hub install + dev instructions per Even Hub overview docs).\n10. Run `npm install` (or `npm ci` if lockfile is stale) and then `npm run typecheck`. Both MUST succeed. Commit only after they do.\n\nDo NOT pre-fill cursor/voice/glasses module logic — that's the next layer of workers. Your job is the substrate they all build on.\n\nMeasurement note: in `## Measurements` of your handoff, report `typecheck-exit` = 0 and `node-modules-present` = true.",
      "pathsAllowed": [
        "package.json",
        "package-lock.json",
        "tsconfig.json",
        "tsconfig.bridge.json",
        "vite.config.ts",
        "index.html",
        "README.md",
        "src/**",
        "public/**",
        ".env.example"
      ],
      "pathsForbidden": [
        "docs/**",
        ".orchestrate/**"
      ],
      "acceptance": [
        "Old bridge/hub/shared files listed in scopedGoal step 1 are removed.",
        "`package.json` matches spec §4 (no dotenv/ws/zod/tsx/@types/ws; @deepgram/sdk added; bridge scripts gone).",
        "Directory tree under `src/` matches spec §3 exactly (every listed file exists).",
        "`src/storage/storage.ts` and `src/shared/events.ts` are fully implemented; all other new modules contain only typed stubs that throw `not implemented`.",
        "`npm run typecheck` exits 0.",
        "`npm install` ran cleanly (`node_modules/` present and `package-lock.json` updated)."
      ]
    },
    {
      "name": "cursor-client",
      "type": "worker",
      "model": "composer-2.5",
      "dependsOn": ["scaffold"],
      "scopedGoal": "Fully implement `src/cursor/` against the contract in docs/spec.md §5. Concretely:\n\n1. `src/cursor/types.ts` — typed mirrors of `Agent`, `Run`, `RunStatus`, `RunStreamEvent` (the SSE event union: status / assistant / thinking / tool_call / result / heartbeat / error / done), `CreateAgentInput`, `CreateRunInput`. Strict types, no `any` in exports.\n2. `src/cursor/auth.ts` — `getCursorApiKey()` / `setCursorApiKey(v)` thin wrappers over `KeyStore` from `src/storage/storage.ts`, key name `cursor.apiKey`. Also export `basicAuthHeader(apiKey: string): string` returning `Basic ${btoa(apiKey + \":\")}` (use the browser `btoa`).\n3. `src/cursor/client.ts` — `class CursorClient { constructor(apiKey: string) }` with:\n   - `me(): Promise<{ apiKeyName: string; userEmail: string; createdAt: string }>` — `GET /v1/me`. Used by SignIn to validate the key.\n   - `listAgents(opts?: { limit?: number; cursor?: string; includeArchived?: boolean }): Promise<{ items: Agent[]; nextCursor: string | null }>`.\n   - `getAgent(id: string): Promise<Agent>`.\n   - `createAgent(input: CreateAgentInput): Promise<{ agent: Agent; run: Run }>`.\n   - `listRuns(agentId: string, opts?: { limit?: number; cursor?: string }): Promise<{ items: Run[]; nextCursor: string | null }>`.\n   - `getRun(agentId: string, runId: string): Promise<Run>`.\n   - `createRun(agentId: string, input: CreateRunInput): Promise<{ run: Run }>`.\n   - `cancelRun(agentId: string, runId: string): Promise<{ id: string }>`.\n   - `streamRun(agentId: string, runId: string, opts?: { lastEventId?: string; signal?: AbortSignal }): AsyncIterable<RunStreamEvent>` — open `/v1/agents/{id}/runs/{runId}/stream` with `Accept: text/event-stream` and parse the SSE byte stream manually (fetch + ReadableStream reader, since browser EventSource cannot set Authorization headers). Each yielded event MUST be a discriminated union value matching the `RunStreamEvent` type. Track the most recent `id:` line; on disconnect, the caller may resume by passing that id back. On HTTP 410, throw a typed `StreamExpiredError` so the caller can switch to polling.\n   - `listRepositories(): Promise<{ items: { url: string }[] }>` — `/v1/repositories`, best-effort, marked in JSDoc as rate-limited.\n\nAll request methods MUST throw a typed `CursorApiError` (extends Error with `status: number`, `code?: string`, `message: string`) on non-2xx; parse the JSON body for `error.code`/`error.message` when present. Add a small `withRetry(fn)` helper that retries `429` and `5xx` with exponential backoff (max 3 attempts).\n\n4. Add `src/cursor/__tests__/commands.test.ts`... wait no, this task is cursor only. Add `src/cursor/__tests__/client.test.ts` with vitest covering: basicAuthHeader formatting, error parsing on a mock 401, and SSE-line splitting against a recorded stream fragment. Use `vi.fn()` for `fetch` (no network).\n\n5. Run `npm run typecheck` AND `npm run test -- src/cursor`; both MUST be green. Commit.\n\nDo NOT touch `src/voice/`, `src/glasses/`, or `src/ui/` in this task — workers for those run in parallel on sibling branches.\n\nMeasurement note: in `## Measurements`, report `cursor-test-count` = (number of test cases passing in `src/cursor/__tests__`).",
      "pathsAllowed": [
        "src/cursor/**",
        "package.json",
        "package-lock.json"
      ],
      "pathsForbidden": [
        "src/voice/**",
        "src/glasses/**",
        "src/ui/**",
        "src/storage/**",
        "src/shared/**",
        "src/main.ts",
        "docs/**",
        ".orchestrate/**"
      ],
      "acceptance": [
        "`src/cursor/client.ts`, `types.ts`, `auth.ts` are fully implemented per spec §5.",
        "`streamRun` parses real SSE chunks (no EventSource); throws `StreamExpiredError` on 410.",
        "`CursorApiError` is exported and thrown on non-2xx.",
        "`npm run typecheck` exits 0.",
        "`npm run test -- src/cursor` passes, with at least 3 distinct test cases."
      ],
      "verify": "## Setup\n- Run `npm install` in the repo root.\n\n## Automated\n- `npm run typecheck`\n- `npm run test -- src/cursor`\n\n## Manual\n- Read `src/cursor/client.ts` and confirm each method in spec §5 is present with the documented signature.\n- Confirm `streamRun` does NOT use `new EventSource(...)` (must use fetch + reader so the Authorization header can be set).\n\n## Gotchas\n- API key in Basic Auth is `username:` (empty password), so the header value is `Basic ${btoa(apiKey + \":\")}`. A common mistake is `btoa(apiKey)` alone — verify the colon is present."
    },
    {
      "name": "voice-client",
      "type": "worker",
      "model": "composer-2.5",
      "dependsOn": ["scaffold"],
      "scopedGoal": "Fully implement `src/voice/` against the contract in docs/spec.md §6 and §7. Concretely:\n\n1. `src/voice/types.ts` — `DeepgramTranscript { transcript: string; isFinal: boolean; speechFinal: boolean; words: { word: string; start: number; end: number }[] }`; `CommandToken { kind: 'command'; verb: string; rest: string; raw: string }`; `TextToken { kind: 'text'; text: string }`; `ParsedUtterance { tokens: Array<CommandToken | TextToken>; firstCommand: CommandToken | null }`.\n2. `src/voice/deepgram.ts` — `class DeepgramLive` with constructor `(apiKey: string, opts?: { model?: string; sampleRate?: number })`, methods:\n   - `start(): Promise<void>` — opens `wss://api.deepgram.com/v1/listen?...` (params per spec §6) with `WebSocket` subprotocols `['token', apiKey]` so the key stays out of the URL.\n   - `sendPcm(chunk: ArrayBuffer | Uint8Array): void` — forward audio.\n   - `keepAlive(): void` — start an 8s timer that sends `{type:'KeepAlive'}`.\n   - `close(): Promise<void>` — sends `{type:'CloseStream'}` then closes.\n   - Inherits from a typed `Emitter` (your `src/shared/events.ts`) with events: `open`, `transcript: DeepgramTranscript`, `error: Error`, `close`.\n   - Parses `Results` messages into `DeepgramTranscript` and emits.\n3. `src/voice/mic.ts` — `class BrowserMic` with `start(onPcm: (frame: Int16Array) => void): Promise<void>` and `stop(): Promise<void>`. Uses `getUserMedia({audio:true})`, an `AudioContext({sampleRate:16000})`, and an inline `AudioWorkletProcessor` (registered via Blob URL) that downsamples to 16k mono PCM Int16 and posts frames to the main thread. Make sure the worklet code works when bundled by Vite (don't `import` it; build the worklet source as a string literal and `Blob`-load it).\n4. `src/voice/commands.ts` — `parseTranscript(text: string): ParsedUtterance` implementing every row of spec §7:\n   - tolerate the literal word `slash` (`'slash new fix it'` → command `new`),\n   - case-insensitive verb,\n   - multi-word verbs `follow up` and `sign in` collapse to `followup` / `signin`,\n   - rest-of-line is `rest`,\n   - tokens-after-command go into `rest`, tokens-before become a leading TextToken.\n5. Tests `src/voice/__tests__/commands.test.ts` covering: every row of the table in spec §7, `slash`-instead-of-`/` variants, mixed case, and trailing whitespace. Aim for ≥10 cases.\n6. Tests `src/voice/__tests__/deepgram.test.ts` using `vi.stubGlobal('WebSocket', …)` covering: subprotocol passed correctly, `sendPcm` forwards bytes, `Results` JSON parses into a `transcript` event.\n7. Run `npm run typecheck` and `npm run test -- src/voice`; both MUST pass. Commit.\n\nDo NOT touch `src/cursor/`, `src/glasses/`, or `src/ui/`.\n\nMeasurement note: report `voice-test-count` in `## Measurements`.",
      "pathsAllowed": [
        "src/voice/**",
        "package.json",
        "package-lock.json"
      ],
      "pathsForbidden": [
        "src/cursor/**",
        "src/glasses/**",
        "src/ui/**",
        "src/storage/**",
        "src/shared/**",
        "src/main.ts",
        "docs/**",
        ".orchestrate/**"
      ],
      "acceptance": [
        "`src/voice/deepgram.ts`, `mic.ts`, `commands.ts`, `types.ts` are fully implemented per spec §6 and §7.",
        "Deepgram WebSocket uses subprotocol auth (`['token', apiKey]`); API key never appears in the URL string.",
        "`parseTranscript` handles `/new`, `slash new`, `/follow up`, `/sign in`, `/cancel`, `/refresh`, `/select N`, `/open`, and free text.",
        "`npm run typecheck` exits 0.",
        "`npm run test -- src/voice` passes, with at least 10 commands test cases and 3 deepgram test cases."
      ],
      "verify": "## Setup\n- `npm install`\n\n## Automated\n- `npm run typecheck`\n- `npm run test -- src/voice`\n\n## Manual\n- Read `src/voice/deepgram.ts` and confirm the `new WebSocket(url, ['token', apiKey])` pattern is used (NOT `?api_key=` in the URL).\n- Read `src/voice/commands.ts` against spec §7 table and verify each row has a test case.\n\n## Gotchas\n- Browser AudioContext input is Float32 at the device sample rate. Resample to 16 kHz Int16 explicitly; trusting `sampleRate:16000` on the context alone has known browser bugs (Safari ignores it)."
    },
    {
      "name": "glasses-client",
      "type": "worker",
      "model": "composer-2.5",
      "dependsOn": ["scaffold"],
      "scopedGoal": "Fully implement `src/glasses/` against docs/spec.md §8 and the Even Hub SDK README at `node_modules/@evenrealities/even_hub_sdk/README.md`. Concretely:\n\n1. `src/glasses/pages.ts` — pure functions that build `CreateStartUpPageContainer` / `RebuildPageContainer` payloads for the two pages in spec §8 (`buildAgentListPage(rows: string[], footer: string)` and `buildAgentDetailPage({ title, statusLine, lastDelta, footer })`). All sizes/positions must fit the 576×288 canvas. The list container is the one with `isEventCapture: 1`. Text containers MUST honour the 16-char `containerName` and 1000-char `content` limits documented in the SDK.\n2. `src/glasses/adapter.ts` — `class GlassesAdapter` with:\n   - `init(): Promise<{ available: boolean }>` — calls `waitForEvenAppBridge()` with a 500 ms timeout; if no bridge, sets `available=false` and turns every subsequent call into a logged no-op (`console.debug('[glasses no-op]', …)`). This is what lets the app run in a normal browser for dev preview.\n   - `showAgentList(rows: string[], footer: string): Promise<void>` — on first call uses `createStartUpPageContainer`, afterwards uses `rebuildPageContainer`. Internal `initialized` flag.\n   - `showAgentDetail(args: { title; statusLine; lastDelta; footer }): Promise<void>` — `rebuildPageContainer`.\n   - `updateDetailDelta(lastDelta: string): Promise<void>` — `textContainerUpgrade` on the middle text container only (avoid full rebuilds on every assistant token).\n   - `onSelection(cb: (index: number, name: string) => void): () => void` — wraps `bridge.onEvenHubEvent`, filters for `listEvent`, forwards `(currentSelectItemIndex, currentSelectItemName)`.\n   - `openMic(onPcm: (frame: Uint8Array) => void): Promise<() => void>` — `audioControl(true)`, subscribes to `audioEvent`, returns an unsubscriber that also calls `audioControl(false)`.\n   - `shutdown(): Promise<void>` — `shutDownPageContainer(0)`.\n3. Tests `src/glasses/__tests__/adapter.test.ts` using vitest with `vi.stubGlobal` for `window` and a fake bridge object. Verify: no-op when bridge missing, `showAgentList` uses createStartUpPageContainer first then rebuildPageContainer second, `updateDetailDelta` calls `textContainerUpgrade` with the middle containerID.\n4. Tests `src/glasses/__tests__/pages.test.ts` verifying coordinates and limits (containerTotalNum ≤ 12, no text content > 1000 chars, list rows truncated to 64 chars).\n5. Run `npm run typecheck` and `npm run test -- src/glasses`; both MUST pass. Commit.\n\nDo NOT touch `src/cursor/`, `src/voice/`, or `src/ui/`.\n\nMeasurement note: report `glasses-test-count` in `## Measurements`.",
      "pathsAllowed": [
        "src/glasses/**",
        "package.json",
        "package-lock.json"
      ],
      "pathsForbidden": [
        "src/cursor/**",
        "src/voice/**",
        "src/ui/**",
        "src/storage/**",
        "src/shared/**",
        "src/main.ts",
        "docs/**",
        ".orchestrate/**"
      ],
      "acceptance": [
        "`src/glasses/adapter.ts` and `pages.ts` are fully implemented per spec §8.",
        "`init()` returns `{ available: false }` when no Even Hub bridge is present (browser dev mode) and every method silently no-ops afterwards.",
        "`showAgentList` uses `createStartUpPageContainer` on first call and `rebuildPageContainer` thereafter.",
        "All text container `content` payloads are within 1000 chars; container counts ≤12; text objects ≤8.",
        "`npm run typecheck` exits 0.",
        "`npm run test -- src/glasses` passes."
      ]
    },
    {
      "name": "app-shell",
      "type": "worker",
      "model": "composer-2.5",
      "dependsOn": ["cursor-client", "voice-client", "glasses-client"],
      "openPR": true,
      "scopedGoal": "Merge the three module branches and build the actual UI on top, then open a PR against `main`. Concretely:\n\n1. Your starting branch is `scaffold`'s output. Merge the three sibling branches into it in this order (the upstream handoffs in your prompt will tell you their actual branch names — use those, not placeholders):\n   - `cursor-client` branch\n   - `voice-client` branch\n   - `glasses-client` branch\n   Resolve conflicts by accepting both sides whenever they touch disjoint paths. If they conflict on `package.json`, take the superset of dependencies. After all three merges, run `npm install && npm run typecheck && npm run test` to confirm nothing regressed.\n2. Implement `src/main.ts` as the entry: instantiate `KeyStore`, `GlassesAdapter`, check for stored API keys, and mount either `SignIn` or `App` into `#app`.\n3. Implement `src/ui/SignIn.ts`: two paste fields (Cursor API key, Deepgram API key), a \"Validate & save\" button that calls `CursorClient#me()` to verify the Cursor key (shows the resolved email on success), persists both keys via `KeyStore`, then dispatches a `signedIn` event so `App` can swap views. Include a deep link `https://cursor.com/dashboard/integrations` for the Cursor key and `https://console.deepgram.com/project/_/api-keys` for Deepgram.\n4. Implement `src/ui/App.ts` (top-level component): renders the `VoiceBar` at the top, the `AgentsList` in the middle, and either nothing or `AgentDetail` at the bottom depending on selection. Wires the `GlassesAdapter` so that:\n   - When the agents list refreshes, `glasses.showAgentList(...)` is called.\n   - When an agent is selected (via UI click OR `glasses.onSelection`), `glasses.showAgentDetail(...)` is called and the SSE stream is attached, with assistant deltas piped to `glasses.updateDetailDelta`.\n   - When `/sign out` (or a dedicated button) is triggered, the keys are cleared and `SignIn` is mounted.\n5. Implement `src/ui/AgentsList.ts`: pulls `CursorClient#listAgents`, renders a list of `STATUS · NAME · time-since-update`, polls every 10 s (debounce). Click → select.\n6. Implement `src/ui/AgentDetail.ts`: shows agent metadata + the most recent run's status, a follow-up textarea, a Cancel button (disabled unless run status is non-terminal), and a tailing log of assistant deltas from `streamRun`. On status `FINISHED`/`ERRORED`/`CANCELLED`, close the stream.\n7. Implement `src/ui/VoiceBar.ts`: a big mic button. On press:\n   - acquire mic via `BrowserMic` (preferred) or `GlassesAdapter#openMic` if `available`,\n   - open `DeepgramLive`, pipe PCM to it,\n   - show the live interim transcript inline,\n   - on `speech_final` flush to `parseTranscript`, dispatch the resulting command (`/new`, `/cancel`, `/followup`, `/refresh`, `/select`, `/open`, `/signin`) via the `App`'s command bus.\n   Pressing the mic again stops the stream.\n8. `src/ui/styles.css`: clean, glanceable mobile-first layout, dark theme, big tap targets (≥48 px), system font stack. Status badges colour-coded.\n9. Update `public/app.json` to a minimal Even Hub plugin manifest (`name`, `version`, `entry: \"index.html\"`).\n10. `npm run typecheck && npm run build && npm run test` — ALL three MUST be green.\n11. Open a draft PR against `main`. Title: `Rework evencursor onto Cursor Cloud Agents + Deepgram + Even Hub HUD`. Body: short summary that links to the new docs/spec.md, lists the merged module branches, includes a screenshot of `npm run build` output, and a checklist matching docs/spec.md §9.\n\nDo NOT regress test counts from any of the three module workers — re-run their tests after each merge and after your UI changes.\n\nMeasurement note: report `final-typecheck-exit` = 0, `final-build-exit` = 0, `total-test-count` (vitest's reported test count after `npm run test`) in `## Measurements`.",
      "pathsAllowed": [
        "src/**",
        "public/**",
        "index.html",
        "package.json",
        "package-lock.json",
        "tsconfig.json",
        "vite.config.ts",
        "README.md"
      ],
      "pathsForbidden": [
        "docs/**",
        ".orchestrate/**"
      ],
      "acceptance": [
        "`scaffold`, `cursor-client`, `voice-client`, `glasses-client` branches are all merged into the app-shell branch.",
        "Sign-In screen validates the Cursor API key with `/v1/me` before persisting.",
        "Agents list page calls `CursorClient#listAgents` and renders results; polls every 10 s.",
        "Selecting an agent opens detail view and attaches to `streamRun` SSE.",
        "Voice bar streams mic audio to Deepgram, displays interim transcripts, and dispatches parsed slash-commands.",
        "GlassesAdapter mirrors agents-list and agent-detail to the HUD when available, silently no-ops in browser dev mode.",
        "`npm run typecheck`, `npm run build`, and `npm run test` all exit 0.",
        "A draft PR is opened against `main` with the rework summary."
      ],
      "verify": "## Setup\n- `npm install`\n\n## Automated\n- `npm run typecheck`\n- `npm run test`\n- `npm run build`\n\n## Manual\n- `npm run dev`, open the printed URL; SignIn screen appears.\n- Paste a Cursor API key (validates) and a Deepgram key; AgentsList renders.\n- Click an agent; AgentDetail opens; SSE stream connects (visible in DevTools Network tab as long-lived `text/event-stream`).\n- Press the mic button; speak `slash refresh`; the agents list refreshes.\n\n## Gotchas\n- `streamRun` requires the Authorization header, so the EventSource API won't work; the worker should have already used fetch+reader in `src/cursor/client.ts`.\n- Even Hub WebView may inject `window.evenAppBridge` asynchronously; `init()` must wait briefly before declaring unavailable."
    },
    {
      "name": "verify-app-shell",
      "type": "verifier",
      "verifies": "app-shell",
      "model": "claude-opus-4-7",
      "scopedGoal": "Verify the rework against every acceptance criterion of `app-shell`. Run `npm install && npm run typecheck && npm run test && npm run build` in a fresh checkout of the app-shell branch and capture exit codes. Then exercise the UI under `npm run dev`: confirm SignIn validates with a fake key (mock the `/v1/me` response by patching `CursorClient` if a real key isn't available — but PREFER using a real key from the env var `CURSOR_API_KEY` if it is set in your VM, since this is your own org's key). Confirm that `parseTranscript` round-trips through `VoiceBar`'s command bus by simulating a `speech_final` event (you can dispatch one in DevTools or write a tiny test harness page). Read the opened PR and confirm body + title match the spec. File your verdict per the orchestrate verifier prompt template.",
      "acceptance": [
        "Verification section contains exit-code evidence for typecheck, test, build.",
        "Verification section names which acceptance criteria in `app-shell` were met live vs unit-tested vs blocked.",
        "PR exists on `main` with the required title."
      ]
    },
    {
      "name": "fix-keystore-fallback",
      "type": "worker",
      "model": "composer-2.5",
      "dependsOn": ["app-shell", "verify-app-shell"],
      "startingRef": "orch/evencursor-rework/app-shell",
      "openPR": false,
      "scopedGoal": "Fix the high-severity bug surfaced by `verify-app-shell`: in browser dev mode, the Even Hub SDK's `waitForEvenAppBridge()` always resolves to a non-functional bridge singleton, so `KeyStore` writes to a bridge whose `setLocalStorage` silently no-ops, and the `window.localStorage` fallback is never reached. As a result, the App shell never mounts `VoiceBar` / `AgentsList` after sign-in. Same issue causes `GlassesAdapter` to report `available=true` when the bridge cannot actually round-trip messages, causing every HUD call to silently fail.\n\nConcrete fixes:\n\n1. **`src/storage/storage.ts`** — replace the time-race-only detection with a **capability probe** that performs a real round-trip:\n   - After `waitForEvenAppBridge()` resolves (or its 500 ms timeout fires), if a bridge was returned, do a one-shot probe: `bridge.setLocalStorage('evencursor.bridge-probe', 'ok')` and `bridge.getLocalStorage('evencursor.bridge-probe')`. If `setLocalStorage` returns `false` OR the readback is not `'ok'`, treat the bridge as **unavailable** and fall back to `window.localStorage` for every subsequent call.\n   - Cache the probe result in a private field on `KeyStore` so it runs at most once per instance.\n   - When the bridge IS available, still write through to `window.localStorage` as a redundant local mirror (so a subsequent browser-only reload still has the value); reads prefer the bridge value but fall back to `localStorage` if the bridge returns empty.\n2. **`src/glasses/adapter.ts`** — `init()` should reuse the same probe pattern: detect a working bridge by doing a harmless `getDeviceInfo()` round-trip with a 500 ms timeout, OR by checking the storage probe result via a small shared helper (export `isEvenHubAvailable()` from `src/storage/storage.ts`). Only set `available = true` when the probe succeeded. Otherwise everything no-ops as today.\n3. **Refactor**: move the bridge probe into a shared module so both `storage.ts` and `glasses/adapter.ts` use it. The natural place is a new internal `src/storage/bridge-probe.ts` (or inside `storage.ts` if you prefer not to add a file). Export `getBridgeIfAvailable(): Promise<EvenAppBridge | null>` that returns the bridge only when round-trip works.\n4. **Tests**:\n   - Add `src/storage/__tests__/storage.test.ts` covering:\n     - probe success → values persist via bridge AND mirror to localStorage,\n     - probe failure (setLocalStorage returns false) → fall back to localStorage,\n     - probe failure (readback empty) → fall back to localStorage,\n     - get reads bridge first, then localStorage,\n     - delete (setKey with undefined) clears both layers,\n     - no bridge present (waitForEvenAppBridge throws / times out) → pure localStorage.\n   - Extend `src/glasses/__tests__/adapter.test.ts` (or add a new file) to assert `available=false` when the probe fails, even if the SDK returns a bridge object.\n5. **Live verification** — run `npm run dev`, open headless Chrome (or use the existing `.verifier/diagnose_keystore.mjs` repro at the root if it is still present from the verifier), and confirm:\n   - The `bridge.setLocalStorage` probe returns false (browser dev mode),\n   - The KeyStore falls back to `window.localStorage`,\n   - After sign-in, `.voice-bar` AND `.agents-list` selectors render,\n   - DevTools Network tab shows a `GET /v1/agents` request and the rendered list reflects the response.\n   Capture two screenshots (`/opt/cursor/artifacts/keystore_fix_signin.png` and `/opt/cursor/artifacts/keystore_fix_agentslist.png`) and reference them in your handoff.\n6. **Verifier artifacts cleanup**: delete the `.verifier/` directory committed by the verifier (it should not ship in the final PR). Commit with message `Drop verifier-only artifacts`.\n7. Run `npm run typecheck && npm run test && npm run build` — all MUST stay green; test count should increase (≥ 32 total).\n8. Push to `orch/evencursor-rework/app-shell` (do NOT create a new branch) so PR #2 picks the fix up automatically. Do NOT open a new PR.\n\nMeasurement note: report `total-test-count`, `final-typecheck-exit`, `final-build-exit`, and `live-signin-unlocks-agents` (true|false) in `## Measurements`.",
      "pathsAllowed": [
        "src/storage/**",
        "src/glasses/**",
        "src/ui/**",
        "src/main.ts",
        "package.json",
        "package-lock.json",
        ".verifier/**",
        "docs/spec.md"
      ],
      "pathsForbidden": [
        "src/cursor/**",
        "src/voice/**",
        "src/shared/**",
        ".orchestrate/**"
      ],
      "acceptance": [
        "`src/storage/storage.ts` performs a sentinel round-trip probe and falls back to `window.localStorage` whenever the probe fails.",
        "`src/glasses/adapter.ts` uses the same probe (directly or via shared helper) so `available` is true only when the bridge can actually round-trip.",
        "Storage unit tests cover probe-success, probe-failure, and no-bridge code paths.",
        "Live `npm run dev` → SignIn → App: `.voice-bar` and `.agents-list` selectors render and `GET /v1/agents` fires.",
        "`.verifier/` directory is removed from the branch.",
        "`npm run typecheck`, `npm run test`, `npm run build` all exit 0; test count is ≥ 32 with the new storage tests.",
        "Pushed to `orch/evencursor-rework/app-shell` (PR #2 updated implicitly)."
      ],
      "verify": "## Setup\n- `npm install` against the updated branch.\n\n## Automated\n- `npm run typecheck`, `npm run test`, `npm run build`.\n- Run the new `src/storage/__tests__/storage.test.ts` directly.\n\n## Manual\n- `npm run dev`, open the URL in Chrome, paste a Cursor API key, confirm the AgentsList renders (network tab shows `/v1/agents` 200).\n- In DevTools console: `localStorage.getItem('cursor.apiKey')` returns the pasted key (proves fallback wrote through)."
    },
    {
      "name": "ship-app-shell",
      "type": "worker",
      "model": "composer-2.5",
      "dependsOn": ["fix-keystore-fallback"],
      "startingRef": "orch/evencursor-rework/fix-keystore-fallback",
      "openPR": false,
      "scopedGoal": "Consolidate the fix into the shippable PR. Concretely:\n\n1. Your starting branch is `orch/evencursor-rework/fix-keystore-fallback` — the fully-fixed code from `fix-keystore-fallback` (live-verified by that worker with screenshots at `/opt/cursor/artifacts/keystore_fix_*.png`). Do NOT modify code; this is a shipping task.\n2. Update the existing draft PR #2 (https://github.com/lexyurk/evencursor/pull/2) — change its head to this branch (`orch/evencursor-rework/ship-app-shell`, which after your push is what you'll be on) by closing PR #2 and opening a fresh draft PR against `main` with the SAME title (`Rework evencursor onto Cursor Cloud Agents + Deepgram + Even Hub HUD`) and an updated body that:\n   - Links docs/spec.md.\n   - Lists every merged child branch: scaffold → cursor-client → voice-client → glasses-client → app-shell → fix-keystore-fallback.\n   - Includes the §9 acceptance checklist with the CURRENT post-fix state checked.\n   - References the two artifact screenshots inline: `<img src=\"/opt/cursor/artifacts/keystore_fix_signin.png\" />` and `<img src=\"/opt/cursor/artifacts/keystore_fix_agentslist.png\" />` so the orchestrate tooling uploads them.\n   - Notes that the prior `verify-app-shell` verdict (`verifier-failed`) was resolved by `fix-keystore-fallback` (`live-ui-verified`).\n3. Re-run `npm install && npm run typecheck && npm run test && npm run build` on a fresh checkout of THIS branch to confirm nothing regressed during the consolidation. Report exit codes in `## Measurements`.\n4. Use ManagePullRequest with `action: close` for #2 and `action: create_pr` for the new PR. If the close action is not available, just update PR #2's title/body via `update_pr` and rely on GitHub's automatic branch swap (set branch_name to your branch).\n\nMake no source edits in this task.\n\nMeasurement note: report `final-typecheck-exit`, `final-test-count`, `final-build-exit`, and `pr-url` (the URL of the active PR after consolidation) in `## Measurements`.",
      "pathsAllowed": [
        "package.json",
        "package-lock.json"
      ],
      "pathsForbidden": [
        "src/**",
        "docs/**",
        ".orchestrate/**",
        ".verifier/**"
      ],
      "acceptance": [
        "Active draft PR against `main` carries the fix-keystore-fallback commits.",
        "PR title is exactly `Rework evencursor onto Cursor Cloud Agents + Deepgram + Even Hub HUD`.",
        "PR body lists every merged child branch including fix-keystore-fallback and embeds both artifact screenshots.",
        "`npm run typecheck`, `npm run test`, `npm run build` all exit 0 on this branch.",
        "`final-test-count` ≥ 35."
      ]
    },
    {
      "name": "extras-features-and-simulator",
      "type": "worker",
      "model": "composer-2.5",
      "dependsOn": ["ship-app-shell"],
      "startingRef": "orch/evencursor-rework/ship-app-shell",
      "openPR": true,
      "scopedGoal": "Extend the shipped evencursor app with the features the operator just asked about (new-agent dialog, archive/unarchive/delete, model picker, in-textarea dictation, edit-before-send) and add a faithful in-browser G2 glasses simulator so the HUD can be demoed without hardware. Push to your own branch and open a draft PR against `main` titled `evencursor: agent CRUD, model picker, dictation, edit-before-send, G2 simulator`.\n\n### 1. Cursor API client extensions (`src/cursor/`)\n\nIn `src/cursor/client.ts` add:\n- `archiveAgent(id: string): Promise<void>` → `POST /v1/agents/{id}/archive`\n- `unarchiveAgent(id: string): Promise<void>` → `POST /v1/agents/{id}/unarchive`\n- `deleteAgent(id: string): Promise<void>` → `DELETE /v1/agents/{id}`\n- `listModels(): Promise<CursorModelCatalog>` → `GET /v1/models`. The response is `{ items: ModelEntry[] }` where each entry has `id`, `displayName`, `aliases?: string[]`, `parameters?: { id: string; displayName?: string; values: { value: string; displayName?: string }[] }[]`, and `variants?: { params: { id: string; value: string }[]; displayName: string; isDefault?: boolean }[]`. Don't trust the SDK; type it ourselves in `src/cursor/types.ts` (add `ModelEntry`, `ModelVariant`, `ModelParameter`, `CursorModelCatalog`).\n- Extend `CreateAgentInput` (in `src/cursor/types.ts`) with optional `model?: { id: string; params?: { id: string; value: string }[] }` and forward it on `createAgent` (POST body includes `model` when present, omitted otherwise). Spec §5 references this.\n- Add tests under `src/cursor/__tests__/client.test.ts` (mocked fetch) for archive (returns 200 → resolves; 404 → throws CursorApiError), delete (DELETE method asserted), and listModels parsing of the actual `composer-2.5` shape returned by api.cursor.com (use a recorded fixture string in the test).\n\n### 2. New-agent dialog (`src/ui/NewAgentDialog.ts`)\n\nNew file. A modal dialog with:\n- `prompt` textarea (3+ rows, required). Above it: a small mic button that opens **dictation mode** (see §5) targeted at this textarea. Below it: a character counter.\n- `repositoryUrl` text input. Accept either `https://github.com/owner/repo` or `owner/repo`; normalise to URL on submit. Optional but if present validate the shape.\n- `name` text input (optional; defaulted to first 48 chars of prompt on blur).\n- **Model picker** select. Populated from `client.listModels()` on open; default to the catalog's `isDefault` variant (today that resolves to `composer-2.5` fast=true). Show the `displayName` plus each variant as a sub-option (e.g. `Composer 2.5 (fast)` vs `Composer 2.5 (thinking)`). Persist the user's last choice in `KeyStore` under `agent.lastModel`.\n- `mode` select: `agent` (default) or `plan` (per Cursor API §Create An Agent's `mode` field).\n- Submit button (`Create agent`) that calls `client.createAgent({ prompt, repositoryUrl?, name?, model? })` with `{ mode }` forwarded and on success closes the modal + refreshes the agents list. Cancel button closes without submitting.\n- ESC closes; click on backdrop closes.\n\nWire a `+ New agent` button into the `agents-list-header` (top right, next to Refresh). When pressed, mount the dialog into a portal `<div class=\"modal-portal\"></div>` injected into the app shell.\n\n### 3. AgentDetail upgrades (`src/ui/AgentDetail.ts`)\n\nAdd these affordances to the existing detail view:\n- **Archive / Unarchive** button in the header next to Back. The label flips based on `agent.archived`. Posts to the new client methods and refreshes the parent list.\n- **Delete** button (small, red, tucked at the bottom of the detail panel under the assistant log). Asks `confirm()` first with the agent name. On success, calls back to the parent so it can clear the detail panel and refresh the list.\n- **Run history**: below the meta block, render the last 10 runs from `client.listRuns(agent.id, { limit: 10 })`. Each row: status badge, run id (truncated), createdAt time-since, and a click handler that re-attaches the SSE stream to that run (read-only — older runs are terminal, so just dump the assistant deltas if available; for terminal runs the stream may 410, fall back to listing nothing rather than crashing).\n- **In-textarea dictation** mic on the follow-up prompt (small button inside the textarea label row that opens dictation targeted at the textarea — see §5).\n- **Edit-before-send for voice followups**: when a `/follow up …` voice command is dispatched while this detail is mounted, do NOT immediately POST. Instead, prefill the follow-up textarea with the spoken rest, focus it, scroll it into view, and let the user press Send (or edit first). Expose this via a new `applyVoiceFollowUp(prompt: string): void` returned from `mountAgentDetail` so `App.ts` can call it.\n\n### 4. App-level wiring (`src/ui/App.ts`)\n\n- Wire the new-agent button + the new dialog mount slot.\n- Update the `/new` voice command handling: instead of immediately calling `createAgent`, **open the New-agent dialog** with the spoken prompt prefilled and any parsed `repositoryUrl` (existing `parseNewAgentRest`). The user reviews + edits and presses Create. This is the edit-before-send flow for new-agent voice.\n- Route `/follow up …` to the AgentDetail's `applyVoiceFollowUp` instead of POSTing directly.\n- Add an `/archive` voice command (and a `/delete` voice command with confirm) — extend `src/voice/commands.ts` and `src/ui/VoiceBar.ts` `VoiceCommand` union to include `archive`, `unarchive`, `delete`. `parseTranscript` should recognise `slash archive` / `slash unarchive` / `slash delete` (case-insensitive; multi-word verbs handled via the same collapse logic).\n- Make the agents-list filter understand `archived: false` by default. Add a header toggle (`Show archived`) that, when on, refetches with `includeArchived: true` and shows archived rows with a muted badge.\n\n### 5. Dictation mode (`src/voice/dictation.ts` + VoiceBar extension)\n\nThe current VoiceBar parses the first slash command on every `speech_final`. Add a second mode: **dictation**. Design:\n\n- New helper `class DictationSession` in `src/voice/dictation.ts` constructed with `({ apiKey: string; target: HTMLTextAreaElement | HTMLInputElement; onCommit?: (text: string) => void })`. It owns a `DeepgramLive` + mic (preferring glasses mic when available, but the helper accepts an injected `openMic` callback so VoiceBar can pass the right one). While active:\n  - Interim transcripts are shown as a live preview (italic / muted) inside a small floating box above the target field (insert at caret position is too fragile; preview-then-commit is fine).\n  - On `speech_final`, the finalised text is **appended** to `target.value` (with a single leading space if non-empty) and the preview box clears. The user can then edit the textarea normally.\n  - `stop()` closes the deepgram + mic and removes the floating preview.\n  - The preview box has its own little ✓ Insert / ✗ Cancel buttons so the user can accept or discard the interim text without speaking.\n- Wire two entry points:\n  - VoiceBar gains a `mode` toggle (`Command` ↔ `Dictate`). In Dictate mode, finalised transcripts are NOT parsed as commands; instead they are appended to `document.activeElement` if it is a textarea/input, otherwise shown in the transcript area only.\n  - `NewAgentDialog` and `AgentDetail` both render a small mic icon next to their textarea label that, when pressed, instantiates a `DictationSession` against that specific field (using `getDeepgramApiKey()` and the same `openMic` indirection App.ts has).\n- Tests under `src/voice/__tests__/dictation.test.ts` covering: append on `speech_final`, interim preview text shown, commit clears preview, Cancel discards.\n\n### 6. G2 glasses simulator route (`src/sim/`)\n\nThis is the most important deliverable for the operator's demo. Build a faithful, **separate** route at `/simulator` (Vite handles this automatically if the URL matches a file; use a hash route `#/simulator` to keep config simple).\n\n- New file `src/sim/Simulator.ts` exporting `mountSimulator(root: HTMLElement): () => void` that renders the actual G2 canvas plus a phone iframe side-by-side.\n- New file `src/sim/g2-canvas.ts` exporting `renderG2Canvas(target: HTMLCanvasElement, page: CreateStartUpPageContainer | RebuildPageContainer): void`. It draws every container from `pages.ts` at **1:1 pixel scale** on a 576×288 canvas: 16-shade greyscale palette, list items rendered as rows with the iconic G2 green tint on a near-black background, selection ring around `currentSelectItemIndex`, text containers rendered with system monospace at the correct y offset and content. Use the constants exported from `src/glasses/pages.ts` so the layout matches what real glasses render. Up-scale the on-screen presentation to 2× (1152×576) for readability while drawing at native res.\n- The simulator page has three columns: (1) the live G2 canvas, (2) simulated touchpad controls (Up / Down / Press / Back / Swipe Up / Swipe Down buttons that dispatch the same `listEvent` shape that real glasses fire), (3) a small status panel showing which page is currently mirrored and the rows it sees. Touchpad buttons drive a local `SimulatorBridge` that implements the `EvenAppBridge` subset we use (`createStartUpPageContainer`, `rebuildPageContainer`, `textContainerUpgrade`, `audioControl`, `shutDownPageContainer`, `onEvenHubEvent`, plus stub `getDeviceInfo`/`getUserInfo`/`setLocalStorage`/`getLocalStorage` so the storage probe succeeds and the app boots).\n- Wire `src/main.ts` so that when the URL hash is `#/simulator`, it injects a global `(window as any).__EVENCURSOR_SIM_BRIDGE__` BEFORE bootstrapping. The KeyStore bridge probe must pick this up: extend `src/storage/bridge-probe.ts` to first check for the sim bridge global, and use it as a working bridge if present. This way the same `App` shell runs inside the simulator and its HUD calls are mirrored to the canvas.\n- The simulator page also embeds the phone view (the regular App shell) in a sibling div, sharing the same `KeyStore` so sign-in state persists. Both views drive the same `CursorClient` (still backed by real API or the existing fixtures during testing).\n- Add a small `Open simulator` link in the app header that opens `#/simulator` in a new tab.\n\n### 7. Smoke + verification\n\n- `npm run typecheck`, `npm run test`, `npm run build` all MUST stay green. Test count must increase: add at least the dictation tests (≥3 cases), client.archive/delete/listModels tests (≥3 cases), and a g2-canvas smoke test that renders to an off-screen canvas and asserts the canvas has non-zero pixel data after rendering a non-empty agent list (≥1 case).\n- Live verify with the existing `verifier-app-shell/live_repro.mjs` pattern (Playwright headless) and **also** capture two new artifact PNGs from the simulator route:\n  - `/opt/cursor/artifacts/sim_agents_list.png` — simulator showing the agents-list page on the G2 canvas\n  - `/opt/cursor/artifacts/sim_agent_detail.png` — simulator showing the agent-detail page with a live SSE delta in the status text container\n  Commit a tiny Playwright script that captures both PNGs at `verifier-extras/sim_capture.mjs` for reproducibility.\n- Open a draft PR against `main` with the title above. Body MUST inline both new artifact screenshots using `<img src=\"/opt/cursor/artifacts/sim_*\" />` so the orchestrate tool uploads them, AND link the PR opened by `ship-app-shell` so the reviewer knows this PR stacks on top.\n\n### Constraints\n\n- DO NOT modify any file in `.orchestrate/`, `docs/`, `.verifier/`, or `verifier-app-shell/`.\n- Keep additions to `package.json` minimal — Vite is already there; you don't need a router lib; vanilla hash routing is fine.\n- Strict TS, no `any` in exports, imports at top of files, no inline `import()`.\n- All public functions get real implementations — no `throw new Error('not implemented')`.\n- Voice mic in headless VMs has no audio source; dictation live-flow tests can use a `DeepgramLive` test double, no real audio.\n\nMeasurement note: report `final-typecheck-exit`, `final-build-exit`, `total-test-count`, `simulator-screenshots-captured` (bool), and `new-features-count` (count of distinct new feature buckets shipped: should be 6 — new-agent dialog, archive/delete, model picker, dictation, edit-before-send, simulator).",
      "pathsAllowed": [
        "src/**",
        "public/**",
        "index.html",
        "package.json",
        "package-lock.json",
        "tsconfig.json",
        "vite.config.ts",
        "README.md",
        "verifier-extras/**"
      ],
      "pathsForbidden": [
        "docs/**",
        ".orchestrate/**",
        ".verifier/**",
        "verifier-app-shell/**"
      ],
      "acceptance": [
        "`CursorClient` exposes archiveAgent, unarchiveAgent, deleteAgent, listModels, and `createAgent` accepts an optional `model`.",
        "Phone UI has a `+ New agent` button that opens a modal with prompt + repo + name + **model picker** + agent/plan mode fields.",
        "AgentDetail exposes Archive/Unarchive, Delete, and a 10-item run history list.",
        "Voice grammar adds `/archive`, `/unarchive`, `/delete` verbs and `parseTranscript` recognises them; `/new` and `/follow up` open the edit-before-send confirmation flow instead of executing immediately.",
        "VoiceBar has a Command / Dictate mode toggle; Dictate appends finalised transcripts to the focused text field with an interim preview box and ✓ Insert / ✗ Cancel.",
        "New-agent prompt textarea AND AgentDetail follow-up textarea each have a per-field mic button that opens a DictationSession bound to that field.",
        "`#/simulator` route renders a 576×288 G2 canvas mirroring the same `pages.ts` payloads, plus simulated touchpad buttons that dispatch list-selection events that drive the real App.",
        "Two artifact PNGs captured: `/opt/cursor/artifacts/sim_agents_list.png` and `/opt/cursor/artifacts/sim_agent_detail.png`.",
        "`npm run typecheck`, `npm run test`, `npm run build` exit 0 with `total-test-count` ≥ 42.",
        "Draft PR opened against `main` titled `evencursor: agent CRUD, model picker, dictation, edit-before-send, G2 simulator` with both screenshots embedded inline."
      ],
      "verify": "## Setup\n- `npm install`\n\n## Automated\n- `npm run typecheck`\n- `npm run test`\n- `npm run build`\n- `node verifier-extras/sim_capture.mjs` (renders both simulator screenshots to /opt/cursor/artifacts/)\n\n## Manual\n- `npm run dev`, open `http://localhost:5173/#/simulator`. Verify the G2 canvas mirrors the agents list and that pressing Up/Down/Select on the simulator touchpad scrolls the green selection ring on the canvas AND opens the detail page on the canvas.\n- On the same simulator page, the embedded phone view's `+ New agent` button opens the modal; the model dropdown is populated; clicking the prompt-field mic icon shows a dictation preview box.\n\n## Gotchas\n- Don't try to literally rasterise a TextContainer via the Even Hub SDK's image pipeline. The simulator renders a *visual approximation* of the HUD for human review — close enough to real G2 output that the layout decisions are obvious.\n- The Cursor `/v1/models` shape is `{ items: ModelEntry[] }`, NOT `{ items: string[] }`. The OpenAPI doc lies; trust the actual response (you can curl it with `CURSOR_API_KEY` from the env to confirm)."
    },
    {
      "name": "reverify-app-shell",
      "type": "verifier",
      "verifies": "app-shell",
      "model": "claude-opus-4-7",
      "dependsOn": ["ship-app-shell"],
      "startingRef": "orch/evencursor-rework/ship-app-shell",
      "scopedGoal": "Final live verification of the shippable branch. Run `npm install && npm run typecheck && npm run test && npm run build`; capture exit codes. Then exercise the dev server end-to-end: SignIn → AgentsList renders → click an agent → AgentDetail mounts and a `text/event-stream` connection appears in DevTools Network for `/v1/agents/.../runs/.../stream` → close the stream. If `CURSOR_API_KEY` is set in your VM, use it for the real `/v1/me` validation and live data; otherwise reuse the harness pattern from the previous verifier (mock fetch). File a verdict per the orchestrate verifier template. Expected verdict: `live-ui-verified` if the live flow works; `unit-test-verified` if you can only confirm via automated tests and the harness; `verifier-failed` only for a NEW regression.",
      "acceptance": [
        "Verification section contains current exit codes for typecheck/test/build.",
        "Verification section confirms SignIn → AgentsList renders live (or explains why blocked).",
        "Verification section confirms AgentDetail attaches SSE on agent click (or explains why blocked).",
        "Active PR URL is referenced and its head matches `orch/evencursor-rework/ship-app-shell`."
      ]
    }
  ]
}
